import React from 'react';
import "./Profile.scss";

export default class Profile extends React.PureComponent {
    render() {
        return (
            <div>
                Profile fragment
            </div>
        )
    }
}